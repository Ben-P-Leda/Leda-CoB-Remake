using UnityEngine;
using System.Collections;

public class BackgroundLayer : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //Diagnostics.DiagnosticsDisplay.SetDiagnostic("zzz", GetComponent<SpriteRenderer>().sprite.bounds.min.ToString());
        //Diagnostics.DiagnosticsDisplay.SetDiagnostic("zzz1", GetComponent<SpriteRenderer>().sprite.bounds.max.ToString());
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
